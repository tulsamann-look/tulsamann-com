{
  "name": "tulsamann-com",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "description": "Personal site for Tulsa Mann — Look Consulting and Claude implementation work.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "pnpm audit:cluster && astro check && astro build && pnpm build:og:all",
    "build:fast": "astro check && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "test:scripts": "vitest run scripts/",
    "audit:cluster": "node scripts/audit-cluster-links.mjs",
    "build:og": "node scripts/build-og-image.mjs",
    "build:og:all": "node scripts/build-og-image.mjs --all",
    "build:playbook": "node scripts/build-playbook-pdf.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/mdx": "^4.0.0",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.2.1",
    "@fontsource-variable/geist": "^5.2.8",
    "@fontsource-variable/geist-mono": "^5.2.7",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^5.1.0",
    "sharp": "^0.34.5",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.0"
  },
  "devDependencies": {
    "playwright": "^1.49.0",
    "vitest": "^4.1.5"
  },
  "engines": {
    "node": ">=20.3.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp",
      "esbuild"
    ]
  }
}
